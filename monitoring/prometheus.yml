global:
  scrape_interval: 15s

scrape_configs:
  - job_name: geotrail-backend
    metrics_path: /api/actuator/prometheus
    static_configs:
      - targets: ['backend:8080']
        labels:
          application: geotrail   # label Grafana dashboard 4701 filters its panels on
