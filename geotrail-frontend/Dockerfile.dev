# Dev image: ng serve with live reload (use with docker-compose.dev.yml)
FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .

EXPOSE 4200

# --poll is required for file watching when the source is bind-mounted on Windows/macOS Docker
CMD ["npm", "run", "start:docker"]
